// project import
import projects from './projects';
import dashboard from './dashboard';
import products from './products';
import employees from './employees';

const menuItems = {
  items: [dashboard, projects, products, employees],
};

export default menuItems;
