import PropTypes from 'prop-types';
import React, { Component } from 'react';

export class FileDetail extends Component {
  static propTypes = {};

  render() {
    return <div>FileDetail</div>;
  }
}

export default FileDetail;
